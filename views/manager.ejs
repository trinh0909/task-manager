<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="/stylesheets/fontawesome-free-6.5.1-web/css/all.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
        <a class="navbar-brand mt-3" href="#">Navbar</a>
        <button class="navbar-toggler ml-auto mt-2 mb-2" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse mt-3" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <% if(session.user.access == 1) { %>
                    <a class="nav-item nav-link" href="/users/manager/1">Công việc chưa giao</a>
                    <a class="nav-item nav-link" href="/users/manager/2">Công việc đã giao</a>
                    <a class="nav-item nav-link" href="/users/manager/3">dữ liệu khách hàng</a>
               <% } %>
               <% if(session.user.access == 2 || session.user.access == 3) { %>
                <a class="nav-item nav-link" href="/users/manager/1">Công việc cần hoàn thành</a>
                <a class="nav-item nav-link" href="/users/manager/2">Công việc đã hoàn thành</a>   
           <% } %>
              <!-- <a class="nav-item nav-link active" href="#">Home <span class="sr-only">(current)</span></a>
              <a class="nav-item nav-link" href="#">Features</a>
              <a class="nav-item nav-link" href="#">Pricing</a>
              <a class="nav-item nav-link disabled" href="#">Disabled</a> -->
            </div>
          </div>
        </div>
      </nav>
      <% if (session && session.user) { %>
        <p>Welcome, <%= session.user.username %>!</p>
        <p>Welcome, <%= session.user.password %>!</p>
        <p>Welcome, <%= session.user.access %>!</p>
        <!-- <p>Your email: <%= session.user.password %></p> -->
        <!-- Hiển thị thông tin người dùng từ session -->
    <% } else { %>
        <p>Please login to view your profile.</p>
        <!-- Hiển thị thông báo nếu không có dữ liệu session -->
    <% } %>

<p>Số: <%= lc %></p>
</body>
</html>


